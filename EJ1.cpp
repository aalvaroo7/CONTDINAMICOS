#include <iostream>
#include <string>
#include <vector>
using namespace std;
int main(){
vector<int>mivector ; //creo el vector mivector
mivector.pushback(1);//agrego un elemento al vector
mivector.pushback(2); //agrego otro elemento al vector
mivector.pushback(3); //agrego otro elemento al vector
mivector_pushback(4); //agrego otro elemento al vector
cout<<"El vector tiene componentes:"
for(vector<int>::iterator i=mivector.begin())




return 0;
}